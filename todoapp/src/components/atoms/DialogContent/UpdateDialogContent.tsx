
import { DialogContent } from "@mui/material"
import { styled } from '@mui/system';


export const UpdateDialogContent = styled(DialogContent)({
    display: 'flex',
    flexDirection: 'column',
    gap: '30px',
})